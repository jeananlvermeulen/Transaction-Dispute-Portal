# BFF server port (frontend proxies /api here)
BFF_PORT=4000

# ASP.NET Core backend URL
BACKEND_URL=https://localhost:53839

# The React dev server origin (used for CORS)
FRONTEND_ORIGIN=http://localhost:3000

# Set to 'production' in prod — enforces secure cookies and rejectUnauthorized TLS
NODE_ENV=development
